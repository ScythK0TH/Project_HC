<!-- <script>
    export let symbol;      
    export let on_click;   
  
    function handleClick() {
      if (on_click) {
        on_click();  
      }
    }
  </script>
  
  <button style="background-color: black;" class="button-container" type="button" on:click={handleClick}>
    {symbol}
  </button>

  <style>
    .button-container {
            display: flex;
            gap: 10px; 
        }

    button {
        color: white;
        border: none;
        padding: 0.5rem 2rem;
        font-size: 1.5rem;
        box-shadow: 0px 1px 3px rgba(0, 0, 0, 0.25);

    }
    button:hover {
        cursor: pointer;
        transform: scale(0.975);
        box-shadow: 0px 3px 3px rgba(255, 0, 0, 0.25),
        inset 0px -2px 3px rgb(0, 0, 0);
    }
</style> -->